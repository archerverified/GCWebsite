/**
 * Centralized layout class constants
 * These reduce duplication and enforce consistency across the site
 */

/** Site-wide horizontal padding scale */
export const PAGE_X_PADDING = "px-4 sm:px-6 md:px-10 lg:px-16 xl:px-20 2xl:px-24";

/** Outer shell wrapper with dark border and mixed radii */
export const SHELL_WRAPPER = "relative bg-white border-2 border-[#35363A] rounded-t-[10px] rounded-b-[20px] overflow-hidden";

/** Yellow accent line at top of shell (pseudo-element) */
export const SHELL_TOP_ACCENT = "before:absolute before:top-0 before:left-0 before:right-0 before:h-[2px] before:bg-[#FEC300] before:z-10";

/** CTA section wrapper with uniform 20px radius */
export const READY_CTA_WRAPPER = "border-2 border-[#35363A] rounded-[20px] overflow-hidden";

